dist/
node_modules/
pnpm-lock.yaml
.cache/
.claude/

# Generated Prisma client (machine-written, never hand-edited).
**/generated/

# The frozen data files are produced by the fetch scripts and must stay
# byte-for-byte as their sources shipped them. Never let a formatter touch them.
prisma/data/quran-uthmani.json
prisma/data/tafseer-muyassar.json

# Markdown is hand-aligned (Arabic glyph widths confuse Prettier's table
# math, and it renders the same on GitHub either way). Leave docs alone.
**/*.md
